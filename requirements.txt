# requirements.txt — pinned so every chapter builds the same way
# (stack pinned as of this book's publication; current LangGraph is v1.x — verify before upgrading)
langgraph==0.2.*
langchain-core==0.3.*
langchain-openai==0.2.*      # swap for your provider's integration package
pydantic==2.*
python-dotenv==1.*           # load OPENAI_API_KEY from .env at startup
httpx==0.27.*                # Ch5: the web_search tool's HTTP client
psycopg[binary]==3.2.*       # Ch6: one Postgres, two roles (checkpointer + memory)
pgvector==0.3.*              # Ch6: vector type adapter for psycopg
langgraph-checkpoint-postgres==2.0.*   # Ch6/Ch11: durable checkpointer
